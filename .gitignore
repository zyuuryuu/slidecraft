# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Python reference code (not part of TS build)
reference/

# Generated SBOMs (use the SBOM workflow / npm run sbom)
sbom-npm.cdx.json
src-tauri/*.cdx.json

# Rust/Tauri build artifacts + generated schemas
/src-tauri/target/
/src-tauri/gen/

# Bundled Node sidecar binary (staged by scripts/stage-node.ps1 before `tauri build`; not source)
/src-tauri/binaries/

# IDE workspace
*.code-workspace

# Test outputs
/test-results/
/playwright-report/

# VitePress build output
docs/.vitepress/dist
docs/.vitepress/cache

# Local-only company template fixture (IP-stripped shell; not committed)
public/templates/slide/CX_sample_MSGothic.pptx
