# Sub Agent 委任ガイド

## 委任に適するタスク

- **新テストケース追加**: 既存テストファイルをテンプレートとして指定
- **Python 参照コード調査**: reference/ 内のコード読解・仕様抽出
- **コードレビュー / 影響分析**: 変更前の影響範囲チェック
- **新アイコン SVG 追加**: icons/ ディレクトリへの追加
- **ドキュメント更新**: README、設計書のステータス更新

## 委任時に指定すべき情報

1. **絶対パス**: `~/Workspace/slidecraft/src/engine/schema.ts`
2. **参照実装**: 既存ファイルをテンプレートとして明示
3. **変更禁止ファイル**: `src/engine/schema.ts`（型定義の中核）
4. **出力形式**: TypeScript、400 行以下
5. **テスト通過**: `npm test` が通ること

## 委任に不適なタスク（メインセッションで実施）

- `schema.ts` の型定義変更（全モジュールに波及）
- `layout-engine.ts` の座標計算ロジック変更
- 複数 engine/ ファイルにまたがるリファクタ
- Tauri IPC / Rust バックエンドの変更
